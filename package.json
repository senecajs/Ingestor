{
  "name": "@seneca/ingestor",
  "version": "0.0.1",
  "description": "Seneca ingestor plugin — reads files from a storage folder and writes structured entities.",
  "main": "dist/Ingestor.js",
  "type": "commonjs",
  "types": "dist/Ingestor.d.ts",
  "license": "MIT",
  "author": "Richard Rodger (http://richardrodger.com)",
  "contributors": [
    "Richard Rodger (http://richardrodger.com)"
  ],
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "test-some": "NODE_OPTIONS=--experimental-vm-modules jest -t",
    "test-watch": "NODE_OPTIONS=--experimental-vm-modules jest --coverage --watchAll",
    "watch": "tsc -w -d",
    "build": "tsc -d",
    "doc": "seneca-doc -p seneca-entity",
    "prettier": "prettier --write --no-semi --single-quote src/**/*.ts test/*.js",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "clean": "rm -rf node_modules dist package-lock.json yarn.lock",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i --registry=http://registry.npmjs.org && npm run repo-publish-quick",
    "repo-publish-quick": "npm run prettier && npm run build && npm run doc && npm test && npm run repo-tag && npm publish --access public --registry=https://registry.npmjs.org"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Rarfael/Ingestor"
  },
  "keywords": [
    "seneca",
    "plugin",
    "ingestor",
    "pdf",
    "xlsx"
  ],
  "peerDependencies": {
    "@seneca/entity-util": ">=2",
    "seneca": ">=3",
    "seneca-entity": ">=25",
    "seneca-promisify": ">=3"
  },
  "devDependencies": {
    "@seneca/doc": "^7.2.0",
    "@seneca/maintain": "^0.1.0",
    "@seneca/s3-store": "^2.5.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.19.41",
    "@types/pdf-parse": "^1.1.5",
    "docx": "^9.7.1",
    "dotenv": "^16.4.5",
    "esbuild": "^0.20.1",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "seneca-msg-test": "^4.1.0",
    "seneca-store-test": "^5.2.0",
    "typescript": "^5.3.3"
  },
  "files": [
    "README.md",
    "CHANGES.md",
    "LICENSE",
    "src",
    "dist"
  ],
  "dependencies": {
    "file-type": "^14.7.1",
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5",
    "xlsx": "^0.18.5"
  }
}
